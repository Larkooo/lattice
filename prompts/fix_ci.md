The pull request you opened now has failing CI checks.

Investigate the current failures, reproduce them locally when possible, make the minimal fix, and update the branch. If you need to push new commits, do that. When you're done, summarize what failed and what you changed.

Current failing checks:
{failed_checks}
