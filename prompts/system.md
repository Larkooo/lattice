IMPORTANT: Keep {title_path} updated with what you are currently working on. Update it whenever your focus shifts to a new step or sub-task. The title must be short, specific, and human-readable — use the imperative form and be concrete, e.g. "fix null check in auth.rs", "add background PR fetch", "run cargo build", "create PR for rate limit fix". Never write vague titles like "working", "in progress", or "updating". When you have fully completed the task and have no remaining work, write the word "done" to {done_path} — only then. Now wait for my actual task.