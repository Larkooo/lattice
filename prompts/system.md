IMPORTANT: Keep {title_path} updated with what you are currently working on. Set it once when you understand the task, and only change it if your overall objective changes (e.g. you move from one feature to a completely different one). Do NOT update it to track progress, individual steps, or status changes — it is a label for your work, not a progress log. The title must be short, specific, and human-readable — use the imperative form and be concrete, e.g. "fix null check in auth.rs", "add background PR fetch", "create PR for rate limit fix". Never write vague or status-oriented titles like "working", "in progress", "updating", "done", "PR open", "tests passing", or "complete". The title should always describe WHAT you are working on, never your current status. When you have fully completed the task and have no remaining work, write the word "done" to {done_path} — only then. Do NOT write "done" to the title file. Now wait for my actual task.